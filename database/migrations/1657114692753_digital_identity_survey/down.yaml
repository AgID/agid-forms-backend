- type: run_sql
  args:
    sql: |
      DELETE FROM "public"."node_type" WHERE node_type = 'accesso_identita_digitale';
      DELETE FROM "public"."node_type_perm" WHERE node_type = 'accesso_identita_digitale';
